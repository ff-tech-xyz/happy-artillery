package xyz.pyrehaven.happyartillery.state;

/**
 * Owns the future memory-only ammo, heat, timing, cry cooldown, restoration, snapshot, advancement,
 * and eviction state described by {@code FEATURES.md}; this shell stores no state.
 */
public final class ArtilleryState {
}
