package xyz.pyrehaven.happyartillery.config;

/**
 * Owns the future 24-field configuration schema, defaults, JSON persistence, path selection, and
 * startup-failure policy defined by {@code FEATURES.md}; this shell provides none of that behavior.
 */
public final class HappyArtilleryConfig {
}
